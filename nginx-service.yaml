apiVersion: v1
kind: Service
metadata:
  name: sridhar-nginx-service
  namespace: sridhar-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80